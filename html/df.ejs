<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Donations</title>
    <link rel="stylesheet" href="/public/donations.css">
</head>

<body>
    <h1>Donations</h1>
    <form action="/donations" method="POST">

        <!-- Donor ID (User Input) -->
        <label for="donor_id">Donor ID:</label>
        <input type="number" name="donor_id" id="donor_id" placeholder="Enter Donor ID" required>
        <br>

        <!-- Donation Type (User Input) -->
        <label for="donation_type">Donation Type:</label>
        <input type="text" name="donation_type" id="donation_type" placeholder="Enter Donation Type (Cash/Resource)" required>
        <br>

        <!-- Amount (User Input) -->
        <label for="amount">Amount:</label>
        <input type="number" step="0.01" name="amount" id="amount" placeholder="Enter amount (or leave blank)">
        <br>

        <!-- Resource Category (User Input) -->
        <label for="category_id">Resource Category ID:</label>
        <input type="number" name="category_id" id="category_id" placeholder="Enter Resource Category ID (or leave blank)">
        <br>

        <!-- Quantity (User Input) -->
        <label for="quantity">Quantity:</label>
        <input type="number" name="quantity" id="quantity" placeholder="Enter quantity (or leave blank)">
        <br>

        <!-- Allocated To (User Input for Area ID) -->
        <label for="allocated_to">Allocated To (Area ID):</label>
        <input type="number" name="allocated_to" id="allocated_to" placeholder="Enter Area ID">
        <br>

        <button type="submit">Submit Donation</button>
    </form>
</body>

</html>
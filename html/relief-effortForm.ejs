<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Relief Effort Form</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            text-align: center;
            margin: 20px;
            background: linear-gradient(135deg, #89f7fe, #3285f9); 
        }
        form {
            display: flex;
            flex-direction: column;
            align-items: center;
            background: #f4f4f4;
            padding: 20px;
            border-radius: 5px;
            max-width: 400px;
            margin: auto;
        }
        input, select, button {
            margin: 10px;
            padding: 8px;
            width: 100%;
            border: 1px solid #ccc;
            border-radius: 5px;
        }
        button {
            background-color: #007bff;
            color: white;
            cursor: pointer;
        }
        button:hover {
            background-color: #0056b3;
        }
    </style>
</head>
<body>
    <form id="reliefEffortForm" method="post" action="/relief_effort">
        <h2>Relief Effort Form</h2>
        <label for="area_id">Area ID:</label>
        <input type="number" id="area_id" name="area_id" required>
        
        <label for="disaster_type">Disaster Type:</label>
        <input type="text" id="disaster_type" name="disaster_type" required>
        
        <label for="status">Status:</label>
        <select id="status" name="status" required>
            <option value="active">Active</option>
            <option value="inactive">Inactive</option>
        </select>
        
        <label for="volunteer_id">Volunteer ID:</label>
        <input type="number" id="volunteer_id" name="volunteer_id" required>
        
        <label for="resource_id">Resource Category ID:</label>
        <input type="number" id="resource_id" name="resource_id">
        
        <label for="quantity_dispatch">Quantity Dispatch:</label>
        <input type="number" id="quantity_dispatch" name="quantity_dispatch">
        
        <button type="submit">Submit</button>
    </form>
</body>
</html>

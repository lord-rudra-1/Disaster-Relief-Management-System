<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Donations - Records</title>
    <link rel="stylesheet" href="/styles.css"> <!-- Add CSS if needed -->
    <style>
        body { font-family: Arial, sans-serif; margin: 20px; }
        h1 { text-align: center; }
        table { width: 100%; border-collapse: collapse; margin-top: 20px; }
        th, td { border: 1px solid black; padding: 10px; text-align: left; }
        th { background-color: #2196F3; color: white; }
        tr:nth-child(even) { background-color: #e3f2fd; }
    </style>
</head>
<body>
    <h1>Donations - Records</h1>
    
    <% if (donations.length === 0) { %>
        <p style="text-align: center; color: red;">No donations recorded yet.</p>
    <% } else { %>
        <table>
            <tr>
                <th>Donation ID</th>
                <th>Donor ID</th>
                <th>Donation Type</th>
                <th>Amount</th>
                <th>Category ID</th>
                <th>Quantity</th>
                <th>Allocated To</th>
                <th>Donation Date</th>
            </tr>
            <% donations.forEach(donation => { %>
                <tr>
                    <td><%= donation.donation_id || "N/A" %></td>
                    <td><%= donation.donor_id || "N/A" %></td>
                    <td><%= donation.donation_type || "N/A" %></td>
                    <td><%= donation.amount !== null ? donation.amount : "N/A" %></td>
                    <td><%= donation.category_id !== null ? donation.category_id : "N/A" %></td>
                    <td><%= donation.quantity !== null ? donation.quantity : "N/A" %></td>
                    <td><%= donation.allocated_to || "N/A" %></td>
                    <td><%= donation.donation_date || "N/A" %></td>
                </tr>
            <% }) %>
        </table>
    <% } %>

    <br>
    <a href="/home" style="display: block; text-align: center; font-size: 18px;">⬅ Go Back to Home</a>
</body>
</html>
